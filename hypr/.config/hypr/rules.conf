# Window rules
# See:
# https://wiki.hypr.land/Configuring/Window-Rules/
# https://wiki.hypr.land/Configuring/Workspace-Rules/

# Workspace-to-monitor mapping
workspace = 1, monitor:DP-1, default:true, persistent:true
workspace = 5, monitor:DP-1, persistent:true

workspace = 2, monitor:DP-2, default:true, persistent:true
workspace = 4, monitor:DP-2, persistent:true
workspace = 7, monitor:DP-2, persistent:true
workspace = 8, monitor:DP-2, persistent:true
workspace = 9, monitor:DP-2, persistent:true

workspace = 3, monitor:DP-3, default:true, persistent:true
workspace = 6, monitor:DP-3, persistent:true
workspace = 10, monitor:DP-3, persistent:true

# App placement
windowrule = workspace 1 silent, match:class chrome-web\.akiflow\.com__-Default
windowrule = workspace 10 silent, match:class chrome-app\.shortwave\.com__-Default
windowrule = workspace 3 silent, match:class chrome-discord\.com__channels_@me-Default

windowrule = workspace 4 silent, match:class (Alacritty|kitty|com\.mitchellh\.ghostty)
windowrule = workspace 2 silent, match:class zen
windowrule = workspace 6 silent, match:class Spotify
windowrule = workspace 7 silent, match:class obsidian
windowrule = workspace 8 silent, match:class steam

# Keep Steam games on the center gaming workspace.
windowrule = tile on, match:class steam_app.*
windowrule = monitor DP-2, match:class steam_app.*
windowrule = workspace 8 silent, match:class steam_app.*
windowrule = fullscreen on, match:class steam_app.*
